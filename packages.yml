# =============================================================================
# dbt Packages
# MetricFlow is built into dbt Cloud; no package needed there.
# For dbt Core, install dbt-metricflow[databricks] via pip.
# =============================================================================
packages:
  - package: dbt-labs/dbt_utils
    version: ">=1.1.0"
