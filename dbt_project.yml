# =============================================================================
# dbt Project Configuration — Retail Sales Semantic Layer POC
# =============================================================================
name: 'retail_sales_semantic_poc'
version: '1.0.0'

require-dbt-version: ">=1.7.0"

profile: 'retail_sales_semantic_poc'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  retail_sales_semantic_poc:
    staging:
      +materialized: view
      +schema: staging
    marts:
      +materialized: table
      +schema: marts
