# =============================================================================
# dbt Project Configuration — Retail Sales Semantic Layer POC
# Pure metadata project: no SQL models materialized, no schemas created.
# =============================================================================
name: 'retail_sales_semantic_poc'
version: '1.0.0'

require-dbt-version: ">=1.7.0"

profile: 'retail_sales_semantic_poc'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"
